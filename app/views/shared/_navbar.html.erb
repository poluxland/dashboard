<nav class="navbar navbar-expand-lg navbar-dark bg-dark mb-4">
  <div class="container">
    <%= link_to "Impromaq", root_path, class: "navbar-brand", data: { turbo: false } %>

    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#mainNavbar"
            aria-controls="mainNavbar" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="mainNavbar">
      <ul class="navbar-nav ms-auto">

        <!-- Menú OTs -->
        <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle <%= (controller_name == 'ots') ? 'active' : '' %>" href="#" id="otsDropdown"
             role="button" data-bs-toggle="dropdown" aria-expanded="false">
            OTs
          </a>
          <ul class="dropdown-menu dropdown-menu-end" aria-labelledby="otsDropdown">
            <li>
              <%= link_to "Resumen", compact_ots_path, class: "dropdown-item", data: { turbo: false } %>
            </li>
            <li>
              <%= link_to "Tabla", ots_path, class: "dropdown-item", data: { turbo: false } %>
            </li>
            <li>
              <%= link_to "Gráficos", graficos_ots_path, class: "dropdown-item", data: { turbo: false } %>
            </li>
            <li>
              <%= link_to "Backlog", graficos_ots_path, class: "dropdown-item", data: { turbo: false } %>
            </li>
          </ul>
        </li>

        <!-- Indicadores -->
        <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle <%= (%w[monthly_records indicator_readings people].include?(controller_name) || current_page?(monthly_records_path)) ? 'active' : '' %>"
             href="#" id="indicadoresDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
            Indicadores
          </a>
          <ul class="dropdown-menu dropdown-menu-end" aria-labelledby="indicadoresDropdown">
            <li>
              <%= link_to "Registros Mensuales", monthly_records_path,
                          class: "dropdown-item #{current_page?(monthly_records_path) ? 'active' : ''}",
                          data: { turbo: false } %>
            </li>
            <li>
              <%= link_to "Indicadores particulares", indicator_readings_path,
                          class: "dropdown-item #{controller_name == 'indicator_readings' ? 'active' : ''}",
                          data: { turbo: false } %>
            </li>
            <li>
              <%= link_to "Personas", people_path,
                          class: "dropdown-item #{controller_name == 'people' ? 'active' : ''}",
                          data: { turbo: false } %>
            </li>
          </ul>
        </li>

      </ul>
    </div>
  </div>
</nav>
