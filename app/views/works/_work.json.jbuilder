json.extract! work, :id, :fecha, :planta, :numero_cotizacion, :solicita, :supervisor, :hora_inicio, :hora_termino, :nombre, :seguridad, :descripcion, :personal, :created_at, :updated_at
json.url work_url(work, format: :json)
